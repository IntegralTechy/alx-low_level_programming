#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints its name, followed by a new line.
 * @argc: counts of arguments entered to main
 * @argv: an array of strings arguments entered
 *
 * Return: 0
 */

int main(int argc, char *argv[])
{
	void(argc);
	printf("my name is %s\n", argv[0]);
	return (0);
}
